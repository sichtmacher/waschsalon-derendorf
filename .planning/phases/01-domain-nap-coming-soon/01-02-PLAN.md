---
phase: 01-domain-nap-coming-soon
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/pages/impressum.astro
  - src/pages/datenschutz.astro
  - src/components/GoogleMap.astro
autonomous: false
requirements:
  - WEB-08

must_haves:
  truths:
    - "Coming-Soon-Seite zeigt Name, Adresse, Eroeffnungszeitraum und Beschreibung"
    - "Zweisprachig: deutscher Hauptinhalt plus kurzer englischer Satz"
    - "Google Maps laedt erst nach Klick (DSGVO-konform)"
    - "Impressum-Seite ist erreichbar unter /impressum"
    - "Datenschutzerklaerung ist erreichbar unter /datenschutz"
    - "Email-Adresse info@waschsalon-derendorf.de ist sichtbar"
    - "Kein Telefon auf der Seite"
    - "Tab-Titel zeigt Waschsalon Derendorf"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML skeleton with Font component, meta tags, favicon, footer with legal links"
      min_lines: 30
    - path: "src/pages/index.astro"
      provides: "Coming-Soon landing page with all required content"
      min_lines: 40
    - path: "src/pages/impressum.astro"
      provides: "Legal Impressum page (DDG ss5)"
      min_lines: 20
    - path: "src/pages/datenschutz.astro"
      provides: "Datenschutzerklaerung page"
      min_lines: 20
    - path: "src/components/GoogleMap.astro"
      provides: "Click-to-load Google Maps component"
      min_lines: 15
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout import"
      pattern: "import.*BaseLayout"
    - from: "src/pages/index.astro"
      to: "src/components/GoogleMap.astro"
      via: "component import"
      pattern: "import.*GoogleMap"
    - from: "src/layouts/BaseLayout.astro"
      to: "/impressum and /datenschutz"
      via: "footer navigation links"
      pattern: "href=.*/impressum|href=.*/datenschutz"
    - from: "src/components/GoogleMap.astro"
      to: "Google Maps iframe"
      via: "click event creates iframe dynamically"
      pattern: "createElement.*iframe"
---

<objective>
Build all pages and components for the Coming-Soon site: landing page, Impressum, Datenschutz, and the click-to-load Google Maps component.

Purpose: Create the publicly visible Coming-Soon presence with all required content, legal pages, and DSGVO-compliant Maps embed.
Output: Complete set of pages ready for Netlify deployment.
</objective>

<execution_context>
@/Users/hp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-nap-coming-soon/01-CONTEXT.md
@.planning/phases/01-domain-nap-coming-soon/01-RESEARCH.md
@.planning/phases/01-domain-nap-coming-soon/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseLayout, GoogleMap component, and all pages</name>
  <files>src/layouts/BaseLayout.astro, src/components/GoogleMap.astro, src/pages/index.astro, src/pages/impressum.astro, src/pages/datenschutz.astro</files>
  <action>
**1. BaseLayout.astro** (`src/layouts/BaseLayout.astro`):

Create the base HTML layout with:
- Props: `title` (string, default "Waschsalon Derendorf"), `description` (string)
- `<html lang="de">`
- Head: charset, viewport, `<Font cssVariable="--font-body" preload />` (import `Font` from `astro:assets`), favicon link to `/favicon.svg`, title tag, meta description, slot named "head" for page-specific meta
- Import `../styles/global.css`
- Body with `font-body` class, bg-white, text color using the theme `--color-text`
- Main content slot
- Footer with links to `/impressum` and `/datenschutz`, styled subtle (small text, gray, centered)

**2. GoogleMap.astro** (`src/components/GoogleMap.astro`):

Click-to-load Maps component (DSGVO-konform):
- Props: `address` (string)
- Use keyless embed URL: `https://www.google.com/maps?q=${encodeURIComponent(address)}&output=embed`
- Show a placeholder div with:
  - Light gray background (bg-surface), rounded corners, aspect-video ratio
  - Text: "Karte wird von Google Maps geladen."
  - Subtext: "Beim Laden werden Daten an Google uebertragen."
  - Button styled with brand color (#1D4ED8): "Karte laden"
- On button click: replace placeholder with Google Maps iframe (use `<script>` tag with `define:vars`)
- Iframe: width 100%, height 100%, border 0, allowFullscreen, loading lazy, referrerPolicy no-referrer-when-downgrade

**3. index.astro** (`src/pages/index.astro`):

Coming-Soon landing page using BaseLayout with:
- Meta description: "Waschsalon Derendorf -- Dein neuer SB-Waschsalon in Duesseldorf. Eroeffnung 2026."
- Clean, centered layout. Content:
  - Large heading: "Waschsalon Derendorf"
  - Subheading/tagline: "Dein neuer SB-Waschsalon in Derendorf"
  - English one-liner (smaller, slightly muted): "Self-service laundromat -- Opening 2026"
  - Section with address: "Muensterstr. 88, 40625 Duesseldorf" (text, not a link)
  - Opening timeframe: "Eroeffnung 2026" (deliberately vague per user decision)
  - Email: "info@waschsalon-derendorf.de" as mailto link
  - NO phone number (per user decision)
  - GoogleMap component with address="Muensterstr. 88, 40625 Duesseldorf"
- Use brand colors from theme: brand-light for accent sections, brand for interactive elements
- Responsive: looks good on mobile (single column) and desktop (centered, max-width container)

**4. impressum.astro** (`src/pages/impressum.astro`):

Legal Impressum page (DDG ss5) using BaseLayout with:
- Title: "Impressum | Waschsalon Derendorf"
- Heading: "Impressum"
- Content following DDG ss5 requirements:
  - "Angaben gemaess DDG ss5"
  - Name: Use a clear TODO placeholder: "[Vor- und Nachname des Betreibers]"
  - Address: Muensterstr. 88, 40625 Duesseldorf
  - Email: info@waschsalon-derendorf.de
  - Phone: visible placeholder text "[Telefonnummer folgt]"
  - Sections with TODO placeholders for: Handelsregister (if applicable), USt-IdNr (if applicable)
  - EU dispute resolution note (standard Streitschlichtung paragraph)
  - Link back to home page
- Style: clean, readable, sufficient spacing, left-aligned text

**5. datenschutz.astro** (`src/pages/datenschutz.astro`):

Datenschutzerklaerung page using BaseLayout with:
- Title: "Datenschutz | Waschsalon Derendorf"
- Heading: "Datenschutzerklaerung"
- Content covering:
  - Verantwortlicher (same as Impressum contact, with TODO placeholders for name)
  - Hosting (Netlify, Inc. -- mention data processing on Netlify servers, link to Netlify privacy policy)
  - Google Maps (loaded only after active consent click, data transmitted to Google upon loading)
  - Server log files (standard Netlify access logs)
  - SSL-Verschluesselung
  - Rechte der Betroffenen (Auskunft, Berichtigung, Loeschung, Einschraenkung, Widerspruch, Datenportabilitaet)
  - Beschwerderecht bei Aufsichtsbehoerde
  - NO sections about cookies, analytics, or tracking (none are used)
  - NO Google Fonts section (fonts are self-hosted, no data transmission)
  - Link back to home page
- Style: same as Impressum, clean and readable

IMPORTANT:
- Do NOT add a phone number anywhere on public pages (user decision)
- DO include the English one-liner on the index page (user decision: zweisprachig)
- The Google Maps component MUST require a click before loading (DSGVO)
- Use the exact brand colors from the theme (brand, brand-light, surface, text)
- All pages must use BaseLayout for consistent styling and footer
  </action>
  <verify>
    <automated>cd /Users/hp/waschsalon && npm run build 2>&1 | tail -3 && echo "---BUILD---" && test -f dist/index.html && echo "PASS: index.html" || echo "FAIL: index.html" && test -f dist/impressum/index.html && echo "PASS: impressum" || echo "FAIL: impressum" && test -f dist/datenschutz/index.html && echo "PASS: datenschutz" || echo "FAIL: datenschutz" && echo "---CONTENT---" && grep -q "Waschsalon Derendorf" dist/index.html && echo "PASS: name present" || echo "FAIL: name missing" && grep -q "Muensterstr" dist/index.html && echo "PASS: address present" || echo "FAIL: address missing" && grep -q "Self-service" dist/index.html && echo "PASS: english text" || echo "FAIL: english text missing" && grep -q "info@waschsalon-derendorf.de" dist/index.html && echo "PASS: email present" || echo "FAIL: email missing" && echo "---DSGVO---" && grep -r "fonts.googleapis.com" dist/ 2>/dev/null && echo "FAIL: external font" || echo "PASS: no external fonts" && grep -q "Karte laden" dist/index.html && echo "PASS: maps click-to-load" || echo "FAIL: maps not click-to-load"</automated>
  </verify>
  <done>All pages build successfully. index.html shows name, address, opening timeframe, email, English text, and click-to-load Maps. Impressum and Datenschutz pages exist with correct legal content. No external font requests. No phone number on any page. Footer links to /impressum and /datenschutz from all pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Coming-Soon website</name>
  <action>
Human verifies the complete Coming-Soon website visually and functionally.

What was built: Complete Coming-Soon website with landing page (name, address, email, Maps, bilingual text), Impressum, and Datenschutzerklaerung. All DSGVO-compliant (self-hosted fonts, click-to-load Maps).

How to verify:
1. Run `cd /Users/hp/waschsalon && npm run preview` to start the local preview server
2. Open the URL shown in the terminal (likely http://localhost:4321)
3. Verify the Coming-Soon page:
   - "Waschsalon Derendorf" heading visible
   - "Dein neuer SB-Waschsalon in Derendorf" tagline
   - English text "Self-service laundromat -- Opening 2026" visible
   - Address "Muensterstr. 88, 40625 Duesseldorf" shown
   - Email "info@waschsalon-derendorf.de" shown (as link)
   - NO phone number anywhere
   - Google Maps placeholder with "Karte laden" button
   - Click "Karte laden" -- map should load showing the address
   - Tab title shows "Waschsalon Derendorf" with basket emoji favicon
4. Check color scheme: blue accents (#1D4ED8), white background, clean/neutral design
5. Check mobile: resize browser to phone width -- layout should adapt cleanly
6. Click "Impressum" in footer -- legal page should load
7. Click "Datenschutz" in footer -- privacy page should load
8. Verify both legal pages have a link back to the home page

Resume signal: Type "approved" if the site looks good, or describe any issues with layout, content, or functionality.
  </action>
  <verify>Human approval</verify>
  <done>User has visually confirmed the Coming-Soon website meets expectations for design, content, and functionality.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- All three pages render: index.html, impressum/index.html, datenschutz/index.html
- index.html contains: "Waschsalon Derendorf", "Muensterstr. 88", "2026", email address, English text, "Karte laden"
- No phone number in any dist/ HTML file
- No external font requests (fonts.googleapis.com / fonts.gstatic.com) in dist/
- Maps iframe only appears after click interaction
- Footer links to /impressum and /datenschutz present on all pages
</verification>

<success_criteria>
A complete, visually polished Coming-Soon website is ready for Netlify deployment. All content matches user decisions exactly: name, address, bilingual text, email, DSGVO-compliant Maps, legal pages. The site looks professional on mobile and desktop with the specified blue/white/gray color palette.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-nap-coming-soon/01-02-SUMMARY.md`
</output>
